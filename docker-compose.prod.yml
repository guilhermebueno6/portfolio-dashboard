# This file is intentionally minimal.
# docker-compose.yml already targets the production build stage directly.
# Use: docker compose up -d --build
#
# (No overrides needed — base file is production-ready.)
